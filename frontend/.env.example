# Frontend (NestJS) — Variables de entorno
# Copia como .env para desarrollo local.

PORT=3000

# URL pública del backend (seminuevos-backend). El navegador la usa para
# llamar a /api/* — nunca se expone aquí una URL ni clave de Supabase.
API_BASE_URL=http://localhost:3001
